- 데이터 넣을 때 꼭 table 직접 만들어서 확인 후 올리기
  reference table이 없다면 table 정의서 참고해서 임의로 만든 후 확인


- 데이터 사이에 ,가 빠진 경우가 많음


- 날짜 넣을 때는 to_date('2022-04-20', 'yyyy-mm-dd') 형식 유의
  만약 날짜의 day가 없다면 2022-07 이렇게 잘린 형태가 아니라 null값


- create에는 not null인데 데이터에는 null인 경우 많음
  일단 null로 처리 후 데이터 들어가는지 확인 후 null로 진행할지, default값 줄지 팀원들과 합의


- 문자열 넣을 땐 '문자열' 이렇게 넣어야함, '' 빼먹지 말기


- seq는 main table로 하고 나머지는 table이름+seq로
  프로젝트떄는 table이름+num 으로 하면 헷갈리니 seq로 통일


- table이 이미 존재하는 경우가 있어 (ex.tblCountry) tblP이름 으로 변경


- reference table 순서대로있으니 차례대로 테이블 만들어서 실행


- varchar2(숫자)에서 데이터 넣기 전 숫자를 적절히 넣기


- 외래키가 not null인데 null이 들어있다면, pk값만 있고 나머지 값이 null이면 의미없는 값이니 그 행은 삭제


- 제일 처음 pk값은 행을 구분하기 위한 숫자일 뿐이니 되도록 순서대로 넣기 > 데이터 넣고 확인할 때 편리(count(*)로 확인)


- insert 문을 작성할 때 통일 > insert all ~ select * ~ 이렇게 작성하면 데이터 넣으면서 확인할 때 불편


- 컬럼명 정할때 실행 안 되는 경우 있으니 꼭 확인하기


- 마지막으로 헷갈렸던 부분이 테이블 하나를 다른 테이블 3개가 동시에 사용할때 였는데, 
  논리적으로 부모자식 관계가 안 맞아도 물리적으로 맞게 진행하면 됨
  원래의 부모를 자식으로 만들고 외래키를 이용해서 구분하는 방법이 최선인 것 같음


- seq는 직접 넣기보다는 nextVal 이용해보기


